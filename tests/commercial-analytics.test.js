import test from'node:test';import assert from'node:assert/strict';import{commercialMetrics}from'../core/radar/visits-deal-room.js';test('analítica comercial usa resultados reales',()=>assert.deepEqual(commercialMetrics({visits:[{stage:'COMPLETED'}],deals:[{stage:'WON'},{stage:'LOST'}]}),{visits_scheduled:0,visits_completed:1,negotiations_active:0,closed_won:1,closed_lost:1,conversion_rate:50}));
