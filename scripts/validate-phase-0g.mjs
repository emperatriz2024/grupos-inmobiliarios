import fs from'node:fs';import assert from'node:assert/strict';const r=f=>fs.readFileSync(new URL(`../${f}`,import.meta.url),'utf8'),db=r('db.js'),app=r('app.js'),html=r('index.html'),sql=r('db/migrations/009_phase_0g_owner_capture_demand.sql');for(const x of['owner_twins','owner_events','capture_pipeline','capture_events','demand_signals']){assert.match(db,new RegExp(x));assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${x}`))}assert.match(db,/const DB_VERSION = 14/);assert.match(app,/renderOwnerOperations/);assert.match(html,/Owner Twin/);assert.match(html,/Pipeline de captacion/);assert.doesNotMatch(sql,/DROP|TRUNCATE/i);console.log('Phase 0G Owner Twin + Capture + Demand Signal: PASS');
