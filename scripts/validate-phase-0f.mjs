import fs from'node:fs';import assert from'node:assert/strict';const r=f=>fs.readFileSync(new URL(`../${f}`,import.meta.url),'utf8'),db=r('db.js'),app=r('app.js'),html=r('index.html'),sql=r('db/migrations/008_phase_0f_property_pipeline_tower.sql');for(const x of['property_twins','commercial_pipeline','pipeline_events']){assert.match(db,new RegExp(x));assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${x}`))}assert.match(db,/const DB_VERSION = (?:14|15)/);assert.match(app,/renderControlTower/);assert.match(html,/Qué hago hoy/);assert.doesNotMatch(sql,/DROP|TRUNCATE/i);console.log('Phase 0F Property Twin + Pipeline + Control Tower: PASS');
