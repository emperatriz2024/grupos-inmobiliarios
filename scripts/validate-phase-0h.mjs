import fs from'node:fs';import assert from'node:assert/strict';const r=f=>fs.readFileSync(new URL(`../${f}`,import.meta.url),'utf8'),db=r('db.js'),sql=r('db/migrations/010_phase_0h_visits_deal_room.sql');for(const x of['visits','visit_events','deals','deal_events']){assert.match(db,new RegExp(x));assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS ${x}`))}assert.match(db,/const DB_VERSION = 15/);assert.doesNotMatch(sql,/DROP|TRUNCATE/i);console.log('Phase 0H Visit Twin + Deal Room: PASS');
